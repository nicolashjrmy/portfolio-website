export type TimelineItemType = {
  title: string;
  company: string;
  location: string;
  period: string;
  type: string;
  color: string;
  achievements: string[];
};

export type TypewriterTextProps = {
  texts: string[]
}